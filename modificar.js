
// se ha añadido un parámetro a la funcion Suma
function calculadoraSuma(op1, op2, op3, op4) {
    let sum;

    sum = op1 + op2 + op3 + op4 ;
    console.log(sum);

    return sum;
}

function calculadoraMulti(op1, op2, op3) {
    let multi;

    multi = op1 * op2 * op3;
    console.log(multi);

    return multi;
}
